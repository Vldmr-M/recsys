version: "3.9"

services:
  api:
    build: ./api
    container_name: recs_api_container
    ports:
      - "8889:8000"

  form:
    build: ./form
    container_name: recs_form_container
    ports:
      - "8888:8501"
